<style type="text/css">
  body {
    -moz-user-select:none;
  }

  #stats {
    width:98%;
    border:1px solid #eee;
    margin:10px;
    margin-top: 0px;
    background:white;
  }
  #stats td, #stats th {
    text-align:center;
  }
  .odd {
    background-color: #eeeef7;
  }
  .even {
    background-color: #fff;
  }

  #table_div {
    overflow:auto;
    height: 320px;
    background-color: white;
    border: solid 1px #eee;
    border-width: 1px;
    overflow: hidden;
  }

  .btn {
    min-height: 50px;
    min-width: 350px;
    margin: 0px;
    margin-bottom: 5px;
    font-size: 1.2em;
    background-color: #6281a7;
    color: #FFF;
    font-weight: normal;
    border: 5px outset #000;
    padding: 10px;
    cursor: pointer;
    background:url(/touchscreentoolkit/lib/images/btn_blue.png) right repeat;
    -moz-user-select:none;
    -moz-user-focus:disabled;
    float: left;
  }

  .btn:hover {
    background:url(/touchscreentoolkit/lib/images/btn_blue_hover.png) right repeat;
    -moz-user-select:none;
    -moz-user-focus:disabled;
  }

  .btn:active {
    background:url(/touchscreentoolkit/lib/images/btn_yellow.png) right repeat;
    border: 3px inset #000;
  }
  #tab{
    width: 700px;
    margin-left: auto;
    margin-right: auto;
}

</style>

<div id="tab" style="background-color: #fff; border-top: 1px #fff solid; margin-top: 0px;">
  <table id="stats" style="margin-bottom: 8px;" cellpadding="5">
     <tr style="color: #fff; background-color: #6281a7;">
      <th colspan="2" style="padding: 10px;">
        Account Settings
      </th>
    </tr>
    <tr>
      <td style="height: 70px; width: 400px;">
        <button class="btn" onclick="window.parent.location='/show_departments?account_id<%= @account.account_id%>&user_id=<%= params[:user_id]
              %>&location_id=<%= session[:location_id] || params[:location_id] %>'">
          <span>
            Manage Account
          </span>
        </button>
      </td>
      <td style="height: 70px;">
        <button class="btn" onclick="window.parent.location='/show_account_medical_scheme?account_id=<%= @account.account_id%>&user_id=<%= params[:user_id]
              %>&location_id=<%= session[:location_id] || params[:location_id] %>'">
          <span>
            Manage Medical Scheme
          </span>
        </button>
      </td>
    </tr>
    <tr>
      <td style="height: 70px;">
        <button class="btn" onclick="window.parent.location='/show_services?account_id<%= @account.account_id%>&user_id=<%= params[:user_id]
              %>&location_id=<%= session[:location_id] || params[:location_id] %>'">
          <span>
            Manage Company Agreement
          </span>
        </button>
      </td>
    </tr>

  </table>
</div>